# Mausam 3D - Environment Configuration
# This application uses Open-Meteo API which does not require an API key.
# No environment variables are required for the weather API.

# Optional: If you want to add a custom API endpoint in the future
# VITE_WEATHER_API_URL=https://api.open-meteo.com/v1
